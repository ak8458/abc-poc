/**
 * Universal Editor support script.
 * Loaded only when the page runs inside the UE context (*.ue.da.live hostname).
 * Add DOM fixups here that are needed specifically for UE authoring —
 * e.g. re-expanding collapsed accordions, resetting carousels to slide 0,
 * or restoring elements that decorateMain strips of their UE attributes.
 */
export default function ue() {
  // placeholder — add block-specific UE fixups here as needed
}
